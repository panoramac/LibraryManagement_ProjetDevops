apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: library-backend-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: library-backend
  endpoints:
    - port: http
      interval: 30s
      path: /actuator/prometheus